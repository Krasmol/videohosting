# 🚀 Quick Start Guide - VideoHost

## Быстрый запуск (5 минут)

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка окружения

Создать файл `.env` (или использовать `.env.example`):

```env
FLASK_ENV=development
SECRET_KEY=your-secret-key-here
DATABASE_URL=sqlite:///instance/video_platform.db
REDIS_URL=redis://localhost:6379/0
```

### 3. Инициализация базы данных

```bash
# Создать таблицы
python -c "from app import create_app, db; app = create_app(); app.app_context().push(); db.create_all()"
```

### 4. Запуск приложения

```bash
# Запустить Flask с SocketIO
python run.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

---

## 📋 Что проверить после запуска

### 1. Главная страница
- ✅ Открыть `http://localhost:5000`
- ✅ Увидеть skeleton loading → затем empty state
- ✅ Проверить адаптивность (изменить размер окна)

### 2. Регистрация и вход
- ✅ Нажать "Регистрация"
- ✅ Создать аккаунт (username, email, password)
- ✅ Войти в систему

### 3. Загрузка видео
- ✅ Нажать "Загрузить" в навигации
- ✅ Заполнить форму:
  - Название
  - Описание
  - Уровень доступа (public/subscriber/sponsor)
  - Файл видео
- ✅ Дождаться загрузки
- ✅ Увидеть видео на главной

### 4. Создание комнаты
- ✅ Перейти в "Комнаты"
- ✅ Нажать "Создать комнату"
- ✅ Выбрать видео
- ✅ Установить лимит участников
- ✅ Создать

### 5. Совместный просмотр
- ✅ Открыть комнату
- ✅ Проверить WebSocket подключение (консоль браузера)
- ✅ Управлять видео (play/pause/seek) - только владелец
- ✅ Написать сообщение в чат
- ✅ Открыть комнату в другой вкладке/браузере
- ✅ Проверить синхронизацию видео
- ✅ Проверить чат между вкладками

---

## 🔧 Troubleshooting

### Проблема: Redis не запускается

**Решение**: Приложение использует `fakeredis` как fallback. Проверьте логи:

```bash
# В консоли должно быть:
# "Redis not available, using in-memory storage"
```

Для production установите Redis:
```bash
# Windows (через Chocolatey)
choco install redis-64

# Linux
sudo apt-get install redis-server

# Mac
brew install redis
```

### Проблема: WebSocket не подключается

**Проверка**:
1. Открыть консоль браузера (F12)
2. Искать сообщения:
   - `WebSocket connected` ✅
   - `WebSocket error` ❌

**Решение**:
- Убедиться что `flask-socketio` установлен
- Проверить что приложение запущено через `python run.py` (не `flask run`)
- Проверить порт 5000 не занят

### Проблема: Видео не загружается

**Проверка**:
1. Размер файла < 500MB (настройка `MAX_CONTENT_LENGTH`)
2. Формат: mp4, avi, mov, mkv, webm
3. Папка `videos/` существует

**Решение**:
```bash
# Создать папку вручную
mkdir videos
mkdir thumbnails
```

### Проблема: База данных не создается

**Решение**:
```bash
# Создать папку instance
mkdir instance

# Запустить миграцию
python -c "from app import create_app, db; app = create_app(); app.app_context().push(); db.create_all(); print('Database created!')"
```

---

## 🎯 Тестовые сценарии

### Сценарий 1: Базовый workflow
1. Регистрация → Вход
2. Загрузка видео (public, с рекламой)
3. Просмотр видео на главной
4. Создание комнаты
5. Совместный просмотр

**Ожидаемый результат**: Все работает без ошибок

### Сценарий 2: Права доступа
1. Создать 2 аккаунта (User1, User2)
2. User1: загрузить видео "subscriber only"
3. User2: попытаться посмотреть → должен увидеть ошибку
4. User2: подписаться на канал User1
5. User2: посмотреть видео → должно работать

**Ожидаемый результат**: Система прав работает корректно

### Сценарий 3: Реклама
1. Создать аккаунт User1
2. Загрузить видео (public, has_ads=true)
3. Выйти из системы
4. Открыть видео как гость
5. Проверить API: `GET /api/videos/1/access`

**Ожидаемый результат**: 
```json
{
  "has_access": true,
  "show_ads": true,
  "access_level": "public"
}
```

### Сценарий 4: WebSocket синхронизация
1. User1: создать комнату
2. User2: присоединиться к комнате
3. User1: play/pause/seek видео
4. User2: видео синхронизируется автоматически
5. User1: написать в чат
6. User2: увидеть сообщение

**Ожидаемый результат**: Полная синхронизация

### Сценарий 5: Лимиты комнат
1. Создать комнату с лимитом 2 участника
2. User1 и User2 присоединяются → OK
3. User3 пытается присоединиться → ошибка "Room is full"

**Ожидаемый результат**: Лимит работает

---

## 📊 Проверка производительности

### WebSocket latency
```javascript
// В консоли браузера
const start = Date.now();
socket.emit('sync_request', { room_id: 1 });
socket.on('state_sync', () => {
  console.log('Latency:', Date.now() - start, 'ms');
});
```

**Ожидаемый результат**: < 100ms

### Загрузка видео
- Файл 10MB → ~5-10 секунд
- Файл 100MB → ~30-60 секунд
- Файл 500MB → ~2-5 минут

### Количество одновременных пользователей
- **Без Redis**: до 10 пользователей
- **С Redis**: до 100+ пользователей

---

## 🐛 Известные проблемы

### 1. Реклама (UI)
**Статус**: Backend готов, UI нет

**Workaround**: Проверять через API:
```bash
curl http://localhost:5000/api/videos/1/access
```

### 2. Kick/Mute в чате
**Статус**: API есть, WebSocket нет

**Workaround**: Использовать REST API:
```bash
curl -X POST http://localhost:5000/api/rooms/1/kick/2 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 3. Thumbnails
**Статус**: Не генерируются автоматически

**Workaround**: Используются placeholder иконки

---

## 📚 Дополнительные ресурсы

### Документация:
- `API_DOCUMENTATION.md` - API endpoints
- `USER_GUIDE.md` - руководство пользователя
- `REFACTORING_COMPLETE_SUMMARY.md` - что было сделано
- `PRODUCTION_REFACTORING_PROGRESS.md` - детальный прогресс

### Код:
- `app/models.py` - модели данных
- `app/services/` - бизнес-логика
- `app/api/` - REST API endpoints
- `app/websocket/` - WebSocket обработчики
- `app/static/css/` - стили
- `app/templates/` - HTML шаблоны

### Тесты:
- `tests/` - unit и integration тесты
- `pytest.ini` - конфигурация pytest

---

## 🚀 Production Deployment

### Требования:
- Python 3.8+
- PostgreSQL (вместо SQLite)
- Redis
- Nginx (reverse proxy)
- Gunicorn (WSGI server)

### Шаги:
1. Установить зависимости
2. Настроить PostgreSQL
3. Настроить Redis
4. Настроить Nginx
5. Запустить через Gunicorn + eventlet

```bash
# Установить production зависимости
pip install gunicorn eventlet psycopg2-binary

# Запустить
gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:5000 run:app
```

### Переменные окружения:
```env
FLASK_ENV=production
SECRET_KEY=<strong-random-key>
DATABASE_URL=postgresql://user:pass@localhost/videohost
REDIS_URL=redis://localhost:6379/0
SOCKETIO_MESSAGE_QUEUE=redis://localhost:6379/1
```

---

## ✅ Чеклист перед запуском

- [ ] Python 3.8+ установлен
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] `.env` файл настроен
- [ ] База данных инициализирована
- [ ] Папки `videos/` и `thumbnails/` созданы
- [ ] Порт 5000 свободен
- [ ] Redis запущен (опционально)

---

## 🎉 Готово!

Приложение должно работать на `http://localhost:5000`

**Первые шаги**:
1. Зарегистрироваться
2. Загрузить видео
3. Создать комнату
4. Пригласить друзей (открыть в другой вкладке)
5. Наслаждаться совместным просмотром!

**Вопросы?** Проверьте:
- Логи в консоли
- Консоль браузера (F12)
- `logs/app.log` (если настроено)

**Удачи!** 🚀
