# 🎨 Frontend Guide - VideoHost Platform

## Обзор

Веб-интерфейс VideoHost - это современное одностраничное приложение (SPA) с красивым дизайном в стиле YouTube/Vimeo.

## 🚀 Запуск

1. Убедитесь, что сервер запущен:
```bash
python run.py
```

2. Откройте браузер и перейдите на:
```
http://127.0.0.1:5000
```

## ✨ Возможности

### Для всех пользователей:
- 🏠 **Главная страница** - просмотр популярных видео
- 🔍 **Поиск** - поиск видео по названию
- 📺 **Просмотр видео** - воспроизведение видео

### Для зарегистрированных пользователей:
- 👤 **Регистрация/Вход** - создание аккаунта и авторизация
- 📤 **Загрузка видео** - загрузка собственных видео
- 📺 **Мой канал** - управление своим каналом
- ❤️ **Подписки** - подписка на каналы
- 💎 **Спонсорство** - поддержка любимых авторов

## 🎨 Дизайн

### Цветовая схема:
- **Основной цвет**: Красный (#ff0000) - как YouTube
- **Фон**: Темный (#0f0f0f)
- **Карточки**: Темно-серый (#1f1f1f)
- **Текст**: Белый (#ffffff)

### Компоненты:

#### Навигация
- Логотип с иконкой
- Поиск по центру
- Кнопки входа/регистрации или меню пользователя

#### Модальные окна
- **Вход** - авторизация пользователя
- **Регистрация** - создание нового аккаунта
- **Загрузка** - загрузка видео с прогресс-баром

#### Карточки видео
- Превью (16:9)
- Название
- Информация о канале
- Метаданные (просмотры, дата)

## 🔧 Технологии

- **HTML5** - структура страниц
- **CSS3** - стилизация (Grid, Flexbox, Animations)
- **Vanilla JavaScript** - логика без фреймворков
- **Font Awesome** - иконки
- **Fetch API** - взаимодействие с backend

## 📁 Структура файлов

```
app/
├── templates/          # HTML шаблоны
│   ├── base.html      # Базовый шаблон
│   └── index.html     # Главная страница
├── static/            # Статические файлы
│   ├── css/
│   │   └── style.css  # Основные стили
│   └── js/
│       └── app.js     # JavaScript логика
└── routes.py          # Flask роуты для HTML
```

## 🎯 Основные функции

### Аутентификация

```javascript
// Вход
await fetch('/api/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username, password })
});

// Регистрация
await fetch('/api/auth/register', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username, email, password })
});

// Выход
await fetch('/api/auth/logout', {
    method: 'POST',
    headers: { 'Authorization': `Bearer ${token}` }
});
```

### Загрузка видео

```javascript
const formData = new FormData();
formData.append('title', 'Название');
formData.append('description', 'Описание');
formData.append('access_level', 'public');
formData.append('video', fileInput.files[0]);

await fetch('/api/videos', {
    method: 'POST',
    headers: { 'Authorization': `Bearer ${token}` },
    body: formData
});
```

## 🎨 Кастомизация

### Изменение цветов

Отредактируйте CSS переменные в `app/static/css/style.css`:

```css
:root {
    --primary-color: #ff0000;      /* Основной цвет */
    --secondary-color: #282828;    /* Вторичный цвет */
    --bg-color: #0f0f0f;          /* Фон */
    --card-bg: #1f1f1f;           /* Фон карточек */
    --text-color: #ffffff;         /* Цвет текста */
}
```

### Добавление новых страниц

1. Создайте HTML шаблон в `app/templates/`
2. Добавьте роут в `app/routes.py`
3. Добавьте JavaScript логику в `app/static/js/app.js`

## 📱 Адаптивность

Интерфейс адаптирован для:
- 💻 Desktop (1400px+)
- 💻 Laptop (1024px+)
- 📱 Tablet (768px+)
- 📱 Mobile (320px+)

## 🔔 Уведомления

Система уведомлений показывает:
- ✅ Успешные операции (зеленый)
- ❌ Ошибки (красный)
- ℹ️ Информация (синий)

## 🚧 В разработке

Следующие функции будут добавлены:
- 📺 Страница просмотра видео с плеером
- 📺 Страница канала с видео автора
- 🔍 Расширенный поиск с фильтрами
- 💬 Комментарии к видео
- 👍 Лайки и дизлайки
- 📊 Статистика просмотров

## 🐛 Отладка

### Проверка API запросов

Откройте DevTools (F12) → Network → XHR для просмотра всех API запросов.

### Проверка токена

```javascript
console.log(localStorage.getItem('token'));
```

### Очистка данных

```javascript
localStorage.clear();
location.reload();
```

## 📚 Полезные ссылки

- [Flask Documentation](https://flask.palletsprojects.com/)
- [Font Awesome Icons](https://fontawesome.com/icons)
- [MDN Web Docs](https://developer.mozilla.org/)

## 🎉 Готово!

Теперь у вас есть полнофункциональный веб-интерфейс для платформы VideoHost!

Перезапустите сервер и откройте http://127.0.0.1:5000 в браузере.
